**Up: [[Study]]

---

Python, Bash, and PowerShell

Add a python script that queries Prometheus + Loki for errors and outputs whether the component is healthy or not. 
Add the script as a cronjob that runs every five minutes on the K3S VM, not the K3S itself.

Add a bash script that monitors the VM health and writes to a file every Sunday at 3pm, CPU, RAM, Disk Usage.